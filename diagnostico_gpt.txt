--- ANÁLISIS DE CLASES EXISTENTES ---

| Clase Java | Clasificación | Motivo |
|------------|----------------|--------|
| Employee | REUTILIZABLE-AS-IS | Contiene atributo `gender` que se necesita para la validación |
| EmployeeRepository | REUTILIZABLE-AS-IS | Se utiliza para buscar y eliminar empleados |
| EmployeeService | REUTILIZABLE-AS-IS | Interfaz que define el contrato del servicio |
| EmployeeServiceImpl | MODIFICABLE | Necesita una validación adicional antes de eliminar un empleado |
| EmployeeController | REUTILIZABLE-AS-IS | No se necesita modificar para esta tarea |
| EmployeeCoreApiApplication | REUTILIZABLE-AS-IS | No se necesita modificar para esta tarea |
| CannotDeleteEmployeeException | NUEVA | Se necesita para manejar el caso de intentar eliminar un empleado femenino |

--- PLAN DE MODIFICACIÓN ---

1. Clase `EmployeeServiceImpl`: Modificar el método `deleteEmployeeById` para agregar una validación antes de eliminar un empleado. Si el empleado es femenino, lanzar la excepción `CannotDeleteEmployeeException`.

2. Clase `CannotDeleteEmployeeException`: Crear esta nueva clase que hereda de `RuntimeException`. Esta excepción se lanzará cuando se intente eliminar un empleado femenino.
