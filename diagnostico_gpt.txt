--- ANÁLISIS DE CLASES EXISTENTES ---

| Clase Java | Package | Clasificación | Motivo |
|------------|---------|----------------|--------|
| Employee | test.core.api.model | REUTILIZABLE-AS-IS | Contiene atributo `gender` |
| EmployeeServiceImpl | test.core.api.service.impl | MODIFICABLE | Debe validar antes de borrar |
| CannotDeleteEmployeeException | test.core.api.exception | NUEVA | Requerida para lanzar excepción |
| EmployeeServiceImplTest | test.core.api.service.impl | NUEVA | Pruebas unitarias de validación |

--- PLAN DE MODIFICACIÓN ---

1. Clase `Employee`: No se requiere ninguna modificación en esta clase. Ya contiene el atributo `gender` que se necesita para la validación.

2. Clase `EmployeeServiceImpl`: Se necesita modificar el método `deleteEmployeeById(Long id)`. Antes de eliminar un empleado, se debe verificar si el género del empleado es "Femenino". Si es así, se debe lanzar una excepción `CannotDeleteEmployeeException`.

3. Clase `CannotDeleteEmployeeException`: Esta es una nueva clase que se necesita para lanzar una excepción cuando se intenta eliminar un empleado de género femenino. Esta clase extiende de `RuntimeException` y toma un mensaje de error como parámetro en su constructor.

4. Clase `EmployeeServiceImplTest`: Esta es una nueva clase que se necesita para las pruebas unitarias. Debe contener pruebas para verificar que la validación de género en el método `deleteEmployeeById(Long id)` funciona correctamente.
